<?xml version="1.0" encoding="UTF-8"?>
	<!--

		Mule NetSuite Cloud Connector Copyright (c) MuleSoft, Inc. All rights
		reserved. http://www.mulesoft.com The software in this package is
		published under the terms of the CPAL v1.0 license, a copy of which
		has been included with this distribution in the LICENSE.txt file.
	-->

	<!--

		This file was automatically generated by the Mule Cloud Connector
		Development Kit
	-->

<mule xmlns="http://www.mulesoft.org/schema/mule/core" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:spring="http://www.springframework.org/schema/beans"
	xmlns:netsuite="http://www.mulesoft.org/schema/mule/netsuite"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                          http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/3.1/mule.xsd
                          http://www.mulesoft.org/schema/mule/netsuite http://www.mulesoft.org/schema/mule/netsuite/1.0-SNAPSHOT/mule-netsuite.xsd">

	<netsuite:config address="127.0.0.1" email="foo@bar.com"
		password="1234" account="myaccount" roleId="3" />
	<!--
		Declare one flow like this one per element you add in your schema. See
		the comment in NetSuiteNamespaceHandlerTestCase for an example how to
		send Mule events directly to this flow.
	-->

	<flow name="serverTime">
		<inbound-endpoint address="vm://salesOrderHold"
			exchange-pattern="request-response" />
		<netsuite:get-server-time />
	</flow>


	<flow name="attachEntity">
		<inbound-endpoint address="vm://attachEntity"
			exchange-pattern="request-response" />
		<netsuite:attach-record sourceEntityType="BUDGET"
			sourceExternalId="500" destinationInternalId="1590"
			destinationEntityType="ACCOUNT" />
	</flow>


	<flow name="deleteEntity">
		<inbound-endpoint address="vm://deleteEntity"
			exchange-pattern="request-response" />
		<netsuite:delete-record recordType="CONTACT" externalId="986"/>
	</flow>

	<flow name="detachEntity">
		<inbound-endpoint address="vm://detachEntity"
			exchange-pattern="request-response" />
		<netsuite:detach-record destinationEntityType="ACCOUNT" destinationExternalId="96" sourceEntityType="ACCOUNT" sourceInternalId="16"/>
	</flow>

	<flow name="getBudgetExchangeRate">
		<inbound-endpoint address="vm://getBudgetExchangeRate"
			exchange-pattern="request-response" />
		<netsuite:get-budget-exchange-rate periodExternalId="986" fromSubsidiaryInternalId="62" toSubsidiaryInternalId="369"/>
	</flow>

	<flow name="getConsolidatedExchangeRate">
		<inbound-endpoint address="vm://getConsolidatedExchangeRate"
			exchange-pattern="request-response" />
		<netsuite:get-consolidated-exchange-rate  periodExternalId="106" fromSubsidiaryInternalId="5689" toSubsidiaryInternalId="4898" />
	</flow>

	<flow name="getCustomizationId">
		<inbound-endpoint address="vm://getCustomizationId"
			exchange-pattern="request-response" />
		<netsuite:get-customization-id type="ACCOUNT"/>
	</flow>


	<flow name="getDeletedEntity">
		<inbound-endpoint address="vm://getDeletedEntity"
			exchange-pattern="request-response" />
		<netsuite:get-deleted-record type="CUSTOMER_PAYMENT" whenExpression="expresion here"/>
	</flow>

	<flow name="getEntities">
		<inbound-endpoint address="vm://getEntities"
			exchange-pattern="request-response" />
		<netsuite:get-entities type="ENTITY_CUSTOM_FIELD"/>
	</flow>

	<flow name="getEntity">
		<inbound-endpoint address="vm://getEntity"
			exchange-pattern="request-response" />
		<netsuite:get-record recordType="CREDIT_MEMO" externalId="982"/>
	</flow>

	<flow name="getItemAvailability">
		<inbound-endpoint address="vm://getItemAvailability"
			exchange-pattern="request-response" />
		<netsuite:get-item-availability/>
	</flow>

	<flow name="getSavedSearch">
		<inbound-endpoint address="vm://getSavedSearch"
			exchange-pattern="request-response" />
		<netsuite:get-saved-search type="ACCOUNT"/>
	</flow>


</mule>
